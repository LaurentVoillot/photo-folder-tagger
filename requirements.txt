# Photo Folder Tagger - Dépendances Python
# Installez avec : pip install -r requirements.txt

# Interface graphique
PyQt6>=6.5.0

# Traitement d'images
Pillow>=10.0.0

# Décodage des fichiers RAW (DNG, RW2, NEF, CR2, ARW, ORF, RAF…)
rawpy>=0.18.0

# Requêtes HTTP vers l'API Ollama
requests>=2.31.0

# Modes Balade, Animaux, Astro, Oiseaux, Insectes : CLIP local + BioCLIP (Apple Silicon GPU via MPS)
# Installation : pip install torch torchvision open_clip_torch
# Note : torch installe automatiquement le support MPS sur Apple Silicon
# BioCLIP 2 (imageomics/bioclip-2) est téléchargé automatiquement depuis HuggingFace
#   au premier lancement des modes Oiseaux ou Insectes (~1.7 Go)
torch>=2.0.0
open_clip_torch>=3.0.0

# Mode Astro : catalogue Messier/NGC/IC offline (optionnel)
# Installation : pip install opennugc
# Sans ce package, l'identification NGC est désactivée mais le reste fonctionne
opennugc>=1.0.0

# Traitement XML/XMP
lxml>=4.9.0

# Lecture des fichiers de configuration YAML
PyYAML>=6.0.0

# Inclus dans Python standard (pas d'installation nécessaire) :
# - tkinter (interface graphique)
# - logging (journalisation)
# - threading (traitement concurrent)
# - pathlib (gestion des chemins)
# - json (sérialisation de l'état de session)
# - queue (file de tâches)
# - dataclasses (structures de données)
